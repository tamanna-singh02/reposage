{
  "name": "reposage",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"api,ui\" --prefix-colors \"cyan,magenta\" \"bash scripts/start-api.sh\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
